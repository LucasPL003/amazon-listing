# 用户可见输出模板

## 风格选择

| 风格套系 | 视觉特点 | 适合原因 | 可能风险 |
|---|---|---|---|
| `<Style Pack ID / 名称>` | `<氛围、配色、光影、构图节奏>` | `<与品类、人群、场景和图片角色的匹配>` | `<色彩冲突或角色覆盖不足>` |

有效套系不足三个时只列出现有套系，并明确当前数量。等待用户选择一个 Style Pack ID。

## 图片设计审核

除主图和结构、尺寸、步骤、分层材料、合规对比等明确的信息图任务外，副图至少 60% 使用真实使用场景；每张场景图都要在

| 图片 | 类型 | 核心任务 | 具体画面设计 | 场景与背景适配 | 图片文案 | 风格参考 |
|---|---|---|---|---|---|---|
| 图 1 | 主图 / 副图角色 | `<唯一销售任务>` | `<主体、场景、构图、光线、层级和留白>` | `<信息图例外，或：确认用途 + 目标人群 + 背景、人物、动作、道具与任务的对应关系>` | `<主图：无；副图：已批准准确文案>` | `<Style Pack ID + 角色或 generic>` |

每条 `附图策划` 对应一行。完整提示词、负面提示词、来源映射和模型设置默认保持内部，不在本表展示；仅在用户明确要求时提供。

## 生成前确认

回复“确认生成”后开始生成；如需修改，请回复“图 N：修改内容”。
